# --- Backend ---
APP_NAME=PaperTradeBot API
BACKEND_PORT=8000
LOG_LEVEL=INFO

# CORS: comma-separated list of allowed origins
# For example, React dev server (3000) or Angular (4200)
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:4200

# --- Alpaca (Paper Trading) ---
# IMPORTANT: Replace with your paper trading credentials from Alpaca
ALPACA_API_KEY_ID=your_alpaca_key_id
ALPACA_API_SECRET_KEY=your_alpaca_secret_key
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# --- Database (PostgreSQL) ---
POSTGRES_DB=papertrade
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Backend connection string to the DB service in docker-compose
# Alternative for development (SQLite): set DATABASE_URL=sqlite:////app/data/app.db in docker-compose.override.yml
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/papertrade

# --- pgAdmin (Web UI) ---
# Login credentials for http://localhost:5050
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin1234
